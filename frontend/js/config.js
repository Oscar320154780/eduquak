// Configuración global de EduQuak.
// En local puedes cambiar esta URL si frontend y backend están separados.
// En Render, si el backend sirve el frontend, se deja vacío para usar el mismo dominio.
window.EDUQUAK_API_URL = window.EDUQUAK_API_URL || "";
